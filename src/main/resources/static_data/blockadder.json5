{
  //working test code for Block Adder based on the spec made by Nixie, with a few tweaks for better JSON style.
  "dirt2": {
    "settings": {
      "copy": "minecraft:dirt"
    }
  },

  "heavy": {
    "settings": {
      "material": "stone",
      // "break_by_hand": false, <- default value
      "break_by_tool": {
        "tool": "fabric:pickaxes",
        "level": 3
      },
      "material_color": "black",
      // "collidable": true, <- default value
      "non_opaque": true, // value doesn't matter, happens if present
      "sounds": "stone",
      "light_level": 0,
      "hardness": 3,
      "resistance": 10000,
      "slipperniness": 1,
      // "breaks_instantly": true <- value doesn't matter, happens if present
      // "drops_nothing": true <- value doesn't matter, happens if present
      // "drops": "namespace:blocks/loot_table_path_here" <- table doesn't exist
      "drops_like": "minecraft:obsidian" //"drops_nothing", "drops", and "drops_like" conflict with each other

    }
  },

  "some_pillar": {
    "style": "axis", //calling it style instead of axis bc not all styles are about rotation
    "settings": {
      "copy": "minecraft:quartz_block"
    }
  },

  "panel": {
    "style": "block_face",
    "settings": {
      "copy": "minecraft:white_banner"
    },
    // "model": "custom", //NYI
    // bounds NYI
  }
}